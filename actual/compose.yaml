services:
  actual:
    image: ghcr.io/actualbudget/actual:26.6.0
    restart: unless-stopped
    environment:
      ACTUAL_PORT: "5000"
    ports:
      - "5000:5000"
    volumes:
      - actualdata:/data

volumes:
  actualdata:
