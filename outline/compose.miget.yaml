services:
  outline:
    x-miget:
      ram: "1024"
    environment:
      DATABASE_URL: ${{db.url}}
      PGSSLMODE: require

  broker:
    x-miget:
      ram: "256"
      private: true

  db:
    x-miget:
      managed: postgres
      cpu: "500m"
      ram: "1Gi"
      storage: "5Gi"
