# Admin/master key - must start with sk-.
LITELLM_MASTER_KEY=sk-       # sk-$(openssl rand -hex 24)

# Encrypts provider API keys stored in the DB. Set BEFORE adding the first
# model; it cannot be rotated afterwards.
LITELLM_SALT_KEY=            # openssl rand -hex 32
