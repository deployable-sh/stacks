# Exactly 32 characters: tr -dc A-Za-z0-9 </dev/urandom | head -c 32
ZITADEL_MASTERKEY=

# BAKED AT FIRST SETUP - set the final domain before first deploy.
ZITADEL_EXTERNALDOMAIN=localhost
ZITADEL_EXTERNALPORT=5000      # 443 on Miget
ZITADEL_EXTERNALSECURE=false   # true on Miget
