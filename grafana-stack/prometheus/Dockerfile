FROM prom/prometheus:latest
# Baked config - the officially documented pattern for environments
# without bind mounts. Edit prometheus.yml to add scrape targets.
COPY prometheus.yml /etc/prometheus/prometheus.yml
