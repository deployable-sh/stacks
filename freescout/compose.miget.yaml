services:
  freescout:
    x-miget:
      ram: "1024"
    environment:
      APP_URL: ${MIGET_PUBLIC_URL}

  db:
    x-miget:
      ram: "1024"
      private: true

volumes:
  freescoutdata:
    x-miget: { size: "10000", type: RWO }
  dbdata:
    x-miget: { size: "5000", type: RWO }
