SECRET_KEY_BASE=        # openssl rand -hex 64

# The app's public host (domain on Miget); FORCE_SSL = the same domain.
HOST=localhost:5000
FORCE_SSL=

# Signature-request emails (point at mailpit:1025 for testing).
SMTP_ADDRESS=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_AUTHENTICATION=plain
