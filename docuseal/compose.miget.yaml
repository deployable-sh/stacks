services:
  docuseal:
    x-miget:
      ram: "1024"
    environment:
      DATABASE_URL: ${{db.url}}

  db:
    x-miget:
      managed: postgres
      cpu: "500m"
      ram: "1Gi"
      storage: "5Gi"

volumes:
  docusealdata:
    x-miget: { size: "5000", type: RWO }
