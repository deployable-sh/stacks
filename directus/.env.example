# Session/crypto secret - set explicitly so sessions survive restarts.
SECRET=                 # openssl rand -hex 32

# Full public URL (set to the app's https domain on Miget).
PUBLIC_URL=http://localhost:5000

# Bootstrap admin (applied on first init only).
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=
