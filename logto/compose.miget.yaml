services:
  logto:
    x-miget:
      ram: "1024"
    environment:
      ENDPOINT: ${{logto.public_url}}
      ADMIN_ENDPOINT: ${{logto.public_url}}
  web-admin:
    x-miget:
      ram: "512"
  db:
    x-miget:
      ram: "512"

volumes:
  dbdata:
    x-miget: { size: "5000", type: RWO }
