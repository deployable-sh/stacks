services:
  logto:
    x-miget:
      ram: "1024"
    environment:
      ENDPOINT: ${MIGET_PUBLIC_URL}
      ADMIN_ENDPOINT: ${MIGET_PUBLIC_URL}
  web-admin:
    x-miget:
      ram: "512"
  db:
    x-miget:
      ram: "512"

volumes:
  dbdata:
    x-miget: { size: "5000", type: RWO }
