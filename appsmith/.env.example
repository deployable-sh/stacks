# Encrypt datasource credentials (AES-256). Optional at first boot, but set
# them from day one: backups and migrations need the original values.
APPSMITH_ENCRYPTION_PASSWORD=        # openssl rand -base64 24
APPSMITH_ENCRYPTION_SALT=            # openssl rand -base64 24
