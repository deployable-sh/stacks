# Basic auth, "user:password" - clients use the same value in
# PREFECT_API_AUTH_STRING.
PREFECT_AUTH_STRING=admin:

# Browser-facing API URL (https://<domain>/api on Miget).
PREFECT_SERVER_UI_API_URL=http://localhost:5000/api
