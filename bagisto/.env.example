# SET EXPLICITLY (base64:32-bytes) - auto-gen per boot would orphan
# sessions/encrypted data on container recreation.
#   echo "base64:$(openssl rand -base64 32)"
APP_KEY=

# The app's public URL (https domain on Miget).
APP_URL=http://localhost:5000
