services:
  bagisto:
    x-miget:
      ram: "2048"
    environment:
      APP_URL: ${MIGET_PUBLIC_URL}

  db:
    x-miget:
      ram: "1024"
      private: true

volumes:
  bagistostorage:
    x-miget: { size: "10000", type: RWO }
  dbdata:
    x-miget: { size: "5000", type: RWO }
