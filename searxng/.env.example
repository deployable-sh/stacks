# Secret key - a long random string. Set it explicitly (otherwise a new
# one is generated on every restart, breaking sessions). Generate with:
#   openssl rand -hex 32
SEARXNG_SECRET=

# The app's public URL (https domain on Miget), with a trailing slash.
SEARXNG_BASE_URL=http://localhost:5000/
