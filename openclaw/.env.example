ANTHROPIC_API_KEY=sk-ant-...                 # or OPENAI_API_KEY / OPENROUTER_API_KEY
AUTH_USERNAME=admin
AUTH_PASSWORD=local-dev-only-change-me       # web UI basic auth
OPENCLAW_GATEWAY_TOKEN=local-dev-only-token  # openssl rand -hex 24; stable API access
# Set to the app's https domain on Miget (after first deploy):
OPENCLAW_ALLOWED_ORIGINS=
