services:
  prestashop:
    x-miget:
      ram: "1024"
    environment:
      PS_DOMAIN: ${MIGET_PUBLIC_HOST}

  db:
    x-miget:
      ram: "1024"
      private: true

volumes:
  psdata:
    x-miget: { size: "10000", type: RWO }
  dbdata:
    x-miget: { size: "5000", type: RWO }
