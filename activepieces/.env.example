AP_ENCRYPTION_KEY=      # openssl rand -hex 16  (32 hex chars)
AP_JWT_SECRET=          # openssl rand -hex 32
AP_REDIS_PASSWORD=      # openssl rand -hex 24  (shared by app + broker)

# The app's public URL (https domain on Miget) - webhook callbacks use it.
AP_FRONTEND_URL=http://localhost:5000
