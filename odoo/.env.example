# Master password - gates database create/drop/backup in the DB manager.
ODOO_ADMIN_PASSWORD=    # openssl rand -base64 24

# True lets the web DB-manager create databases (needs a CREATEDB role).
# Set False if your managed Postgres role can't CREATEDB (pre-create one).
ODOO_LIST_DB=True
