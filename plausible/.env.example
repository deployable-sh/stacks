# The app's public URL (https domain on Miget). Must match exactly - it
# drives link generation and the WebSocket origin check.
BASE_URL=http://localhost:5000

# Secret key base - at least 64 bytes. Generate with:
#   openssl rand -base64 48
# Keep it stable across deploys (changing it breaks sessions and 2FA).
SECRET_KEY_BASE=
