services:
  mariadb-primary:
    x-miget:
      ram: "1024"
      private: true

  mariadb-replica:
    x-miget:
      ram: "1024"
      private: true

volumes:
  primary-data:
    x-miget: { size: "10000", type: RWO }
  replica-data:
    x-miget: { size: "10000", type: RWO }
