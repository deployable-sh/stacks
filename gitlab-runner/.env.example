# gitlab.com or your self-managed GitLab.
CI_SERVER_URL=https://gitlab.com

# Runner authentication token (glrt-...): create the runner in GitLab
# (Settings > CI/CD > Runners > New runner) - tags and run-untagged are
# set there, server-side.
CI_SERVER_TOKEN=
